[build]
  command = "npm run build"  # Command to build your project
  publish = "dist"          # Directory to deploy (output directory after build)

[[redirects]] 
  from = "/*"
  to = "/index.html"
  status = 200
